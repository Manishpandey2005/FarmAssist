{
  "name": "farmassist",
  "version": "1.0.0",
  "description": "AI-powered farmer support platform",
  "main": "backend/app.py",
  "scripts": {
    "dev": "concurrently \"cd backend && python app.py\" \"cd frontend && npm start\"",
    "build": "cd frontend && npm run build",
    "start": "cd backend && python app.py",
    "install-all": "cd backend && pip install -r requirements.txt && cd ../frontend && npm install"
  },
  "keywords": ["agriculture", "farming", "ai", "ml", "react", "flask"],
  "author": "FarmAssist Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}